## 0 题目描述
https://leetcode-cn.com/problems/remove-duplicates-from-sorted-list/


给定一个排序链表，删除所有重复的元素，使得每个元素只出现一次。

示例 1:
输入: 1->1->2
输出: 1->2


示例 2:
输入: 1->1->2->3->3
输出: 1->2->3


## 1 分析条件
## 2 确定问题
去重
## 3 独立思考分析：（前期最长3分钟！！！）
手算思路是什么？
第一次出现的元素添加到备忘录，后续元素对比备忘录，如果备忘录中有，就删除

如何实现呢？
1 额外使用 Java 的有序集合 Set ，直接输出集合中的元素
  Q:使用哪个内部的数据结构呢？ 需求：插入的元素按插入先后排序，
  A:LinkedHashSet

2 对链表的操作，配合备忘录，遍历的同时判断当前操作的元素是否含有备忘录中的元素：如果有，就删除，采用覆盖的策略，如果没有，就添加到备忘录

增加头节点，统一删除操作
## 4 解题报告（独立实现及问题记录）
AC 代码 1 (使用备忘录, 使用了一个指针 + 头结点)
``` java
class Solution {
   public ListNode deleteDuplicates(ListNode head) {
        ListNode temp = head;
        Set<Integer> set = new LinkedHashSet<>();
        // 增加虚拟头节点 统一操作
        ListNode virtuNode = new ListNode( -1);
        virtuNode.next = temp;
        temp = virtuNode;

        while (temp.next != null) {
            if (set.contains(temp.next.val)) {
                // 删除
                temp.next = temp.next.next;
                
            } else {
                set.add(temp.next.val);
                temp = temp.next;
            }
        }

        return head;
    }
}
```

AC 代码 2 (使用了两个指针，当前元素的指针和前一个指针)
``` java
class Solution {
      public ListNode deleteDuplicates(ListNode head) {
        if (head == null || head.next == null) return head;
        // 前一个结点
        ListNode pre = head;
        // 当前处理的结点
        ListNode cur = head.next;

        while (cur != null) {
            if (cur.val == pre.val) { // pre 指针的后面一个元素 更新
                pre.next = cur.next;
                cur = cur.next; // 迭代
            }
            else {                    // 迭代
                cur = cur.next;
                pre = pre.next; 
            }
        }
        
        return head;
    }
}
```

## 5 有没有改进的方法
可以不用 辅助的 set 吗？

### 再次审题
给定一个 **排序链表** , 
既然是有序的，从第二个元素开始遍历，如果和前面的相同，就删掉，否则就前进

## 6 学习别人的分析
执行 时间 为 0 秒的 
TODO
``` java
public ListNode deleteDuplicates(ListNode head) {
    if (head == null || head.next == null)
        return head;

    ListNode pre = head;
    ListNode nh = head;
    head = head.next;

    while (head != null) {
        if (pre.val == head.val) { // 1 元素重复的语句
            pre.next = head.next;
            head = head.next;
        } else {
            pre = head;            // pre 是什么？ 作者的思路是什么？
            head = head.next;
        }
    }

    return nh;
}
```